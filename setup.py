from setuptools import setup

setup(
      name='newCustomTrader',
      version='0.0.2',
      install_requires=['gym', 'numpy', 'pandas', 'matplotlib'],
      description='',
      url='',
      author='Mark Grove',
      author_email='',
      license='unlicense',
      packages=setuptools.find_packages(),
      zip_safe=False
)
