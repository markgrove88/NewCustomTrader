from setuptools import setup

setup(name='New_CustomTrader',
      version='0.0.1',
      install_requires=['gym', 'stable-baselines', 'numpy', 'pandas', 'matplotlib', 'yfinance', 'finta']  # And any other dependencies foo needs
)
